import React from 'react';

function PeopleCard({ name, onPressed, presenter, activeEvents }) {
  // Convert logic from lib/features/home/people_card.dart
  return (
    <div>
      {/* Display the person's name */}
      {name}
    </div>
  );
}

export default PeopleCard;
```

